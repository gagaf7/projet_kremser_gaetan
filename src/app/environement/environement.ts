export const environment = {
  production: false,
  baseUrl: '', // Vide pour utiliser le proxy Angular (requêtes vers /api seront proxiées vers localhost:3000)
  useMock: false // Désactiver le mock pour utiliser l'API réelle
};